/**
 * 默认的通知处理器。
 * 当 vim 框架进行通知时，如不指定通知处理器，默认使用的通知处理器。
 */
const defaultNotifyHandler = 'elementUI';

export { defaultNotifyHandler };

/**
 * 默认的图标处理器。
 * 当 vim 框架进行图标解析时，如不指定图标处理器，默认使用的图标处理器。
 */
const defaultRenderHandler = 'elementUI';

export { defaultRenderHandler };
